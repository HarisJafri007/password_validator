Python Code for Password Validation

A Short Code that defines a function that takes password as input from User and then evaluates it based on the pre-defined criterion.

Password must be atleast 7 Characters long with a minimum of 2 Integers and 2 Special Characters.

Function returns "Strong" if password passess the Criterion.
